.header {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.header__menu {
    @include tablets {
        display: none;
    }
}

.header__socials {
    @include phones {
        display: none;
    }
}
